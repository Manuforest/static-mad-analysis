interface:
  display_name: "Static MAD Deep Analysis"
  short_description: "Human-like narrative and montage video analysis"
  default_prompt: "Use $analyze-static-mad to reconstruct this static MAD's subjects, event-response chains, time-space, material treatments, and audiovisual design with timestamped evidence."

policy:
  allow_implicit_invocation: true
