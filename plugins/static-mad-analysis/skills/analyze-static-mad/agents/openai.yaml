interface:
  display_name: "Subject-Centered MAD Analysis"
  short_description: "Shot-by-shot static-MAD analysis and appreciation"
  default_prompt: "Use $analyze-static-mad to complete a blind shot-by-shot reading of this MAD, then write a natural appreciation grounded in timestamped evidence."

policy:
  allow_implicit_invocation: true
